# Projet Supabase — le même que celui utilisé par l'application actuelle.
# Ces deux valeurs se trouvent dans Supabase → Project Settings → API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Rien d'autre ne vit ici. Le jeton Airtable est une variable de la fonction
# Edge côté serveur (Supabase → Edge Functions → Secrets), il ne doit jamais
# se retrouver dans le bundle envoyé au navigateur.
